# No additional third-party dependencies beyond core stack (streamlit, ultralytics, opencv-python, pillow, reportlab, psycopg2, folium).
# The interactive calibration canvas uses Streamlit's built-in CCv2 (st.components.v2) — no external package needed.
